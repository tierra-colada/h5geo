# to run unittest in usual way:
# `python -m unittest discover h5geopy.tests`
# the folder `h5geopy/tests` must contain this empty `__init__.py` file
# or you will get an error:
# `ImportError: Start directory is not importable: 'h5geopy.tests'`